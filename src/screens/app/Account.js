import React from "react";
import { StyleSheet, View, Text } from "react-native";

const Account = (props) => {
  return (
    <View>
      <Text>Account</Text>
    </View>
  );
};

export default Account;
